{
  "name": "@anvil/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anvil/config": "workspace:*",
    "@anvil/persistence": "workspace:*",
    "@anvil/shared": "workspace:*",
    "fastify": "^5.3.3"
  }
}
