{
  "name": "@anvilstack/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anvilstack/config": "workspace:*",
    "@anvilstack/name-squatting": "workspace:*",
    "@anvilstack/object-store": "workspace:*",
    "@anvilstack/persistence": "workspace:*",
    "@anvilstack/shared": "workspace:*",
    "fastify": "^5.3.3"
  }
}
