{
  "name": "@anvil/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "anvil": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anvil/config": "workspace:*",
    "@anvil/name-squatting": "workspace:*",
    "@anvil/shared": "workspace:*"
  }
}
